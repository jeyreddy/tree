Build Kula Vruksham for production.

```bash
npm run build
```

Output goes to `dist/`. To preview the production build locally before deploying:

```bash
npm run preview
```

Deploy by pushing to the GitHub repo connected to Vercel — it auto-deploys on push to `main`.
