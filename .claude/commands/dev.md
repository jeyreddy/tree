Start the Vite development server for Kula Vruksham.

```bash
npm run dev
```

The app opens at http://localhost:5173. Make sure `.env` exists with valid `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` — copy from `.env.example` if not.
